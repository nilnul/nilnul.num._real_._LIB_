namespace nilnul.num.real
{
	/// <summary>
	/// this is <see cref="real.IOp"/>; but it's not only <see cref="real.IOp"/>, but also <see cref="real.IExpr"/> as the body/logic;
	/// </summary>
	public interface IFunc { }
}
