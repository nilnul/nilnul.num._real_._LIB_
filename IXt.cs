namespace nilnul.num.real
{
	/// <summary>
	/// all real numbers, with infinite<see cref="real.xt_.IInfinite"/> which means not finite.
	/// </summary>
	/// <remarks>
	/// as a relation:
	///		, its realm is infinite, specifically continuum.
	///		, the posInf is not continuous at the lower side, as we cannot find any other xt(except the posInf) that is closer than a given magnitude (<see cref="nilnul.obj._measure._unit_.MagnitudeI"/>).
	///		, it's still dense near infinity;
	///		
	/// </remarks>
	/// alias:
	///		et
	///			, as in "etc"
	///		ext
	///		xt
	public interface IXt 
		
	{ }
}
