using System;

namespace nilnul.num.real.co
{
	/// <summary>
	/// for (a,b), this is b-a
	/// </summary>
	/// <see cref="real.of_.binary_.Departure"/>
	/// <see cref="geometry.linear.grad.IMetric"/>
	/// alias:
	///		displace
	///		deviation
	///		diff,
	///			which is either this or <see cref="ISubtraction"/>
	interface IDisplace:IDifference { }

}
