namespace nilnul.num.real.fn.src.be_
{
	/// <see cref="real.fn.src.be_.IConferring"/>
	/// <summary>
	/// So when a point is contiguous, it might be continuous,  or it might be uncontinuous when its value is outlier.
	/// 
	/// </summary>
	/// <remarks>
	/// <see cref="real.xt.fn.src_.break_.def_.finite_.IUnCongtiguous"/>;
	/// But it cannot be a jump break point that is semicontinuous(<see cref="real.fn.src.be_._contin.semi_"/>); such jump break point is uncontiguous.
	///
	/// </remarks>
	///  vs:<see cref="real.xt.fn.src.be_.IConfluent"/> which includes the case when tow sides approach to the same infinity, But that case is not contiguous, 
	/// alias:
	///		conciliar
	///			,the mediator can be not part of it.
	///		conjoining
	///			,like a joint, which can be hollow
	///		concatenate
	///		concur
	///		confluent
	///		coincident
	///		cofinal
	///		cohesive
	///		coherent
	///		combine
	///		colimit
	///		converge
	///			,reserved for convergence.
	///		coalesce
	///		coagulating
	///
	/// <see cref="real.xt.fn.src.be_.IConfluent"/>
	public interface IConciliar
		:src.IBe
		,
		contig_.ILimitsAreSame
		,
		conflu_.INonInf
		
    {
    }






}
