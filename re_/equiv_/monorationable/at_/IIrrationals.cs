namespace nilnul.num.real.re_.equiv_.monorationable.at_
{
	/// <summary>
	/// Two irrational numbers ξ and η are equivalent if and only if	ξ = [a0, a1, ..., am, c0, c1, c2, ...], η = [b0, b1, ..., bn, c0, c1, ...],the sequence of quotients in ξ after the m-th being the same as the sequence in η after the n-th for some m and n. <see cref="nilnul.num.integer.stream_.nonhead8pos.fractile.re_.SameTail"/>
	/// </summary>
	interface IIrrationals { }
}
