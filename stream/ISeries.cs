namespace nilnul.num.real.stream
{
	/// as this needs the operation of addition, hence
	/// <summary>
	///  this is not applied to all objs as some may lack the addition operater;
	/// </summary>
	/// <see cref="real.stream.cumulus_.Sum"/>
	/// <see cref="real.stream_.slider.cumulus_.SeriesI"/>
	/// vs:
	///		<see cref="real.stream_.slider.subsume_.AddI"/>
	public interface ISeries
		:
		//num.real.stream_.ISlider
		//,
		num.real.stream.cumulus_.ISum
	{

	}




}
