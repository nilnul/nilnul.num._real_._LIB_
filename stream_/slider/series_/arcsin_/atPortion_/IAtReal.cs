namespace nilnul.num.real.stream_.slider.series_.arcsin_.atPortion_
{
	/// <summary>
	/// imaging tgt real number as a quotientBound.
	/// we need the src arg's quotientBound to be small.
	/// 
	/// we need to compute the max derivative of the quotientBound of the real;
	/// Then, shrink the quotientBound, such that
	///		:tgt bound will not exceed given diameter.
	///		:find the arcsin(x) of the mid of the shrunk domain;
	///		:	to make the tgt value reside within given diameter, we need the domain le diameter/max(derivative)
	/// </summary>
	internal class IAtReal
	{

	}


}
